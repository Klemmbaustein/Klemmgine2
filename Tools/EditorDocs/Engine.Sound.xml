<!DOCTYPE docs>
<docs type="bindings" module="engine::sound">
	<functions>
		<getMainSoundContext>
			<description>
				Returns the engine's main sound context. The main context will always have a listener position of 0,0,0.
				Will return null if the engine's sound functions are not enabled.
			</description>
			<return type="engine::sound::SoundContext?" />
		</getMainSoundContext>
	</functions>

	<types>
		<SoundContext>
			<description>
				A class for playing sound. Each sound context represents it's own 3D scene,
				with it's own reverb and sound parameters.
			</description>
			<methods>
				<playSound>
					<description>
						Plays a 2D sound, without any attenuation or panning.
					</description>
					<argument name="sound" type="AssetRef">
						The sound asset to play.
					</argument>
					<argument name="volume" type="float">
						The sound volume, where a value of 1 is how the volume was defined in the sound file.
					</argument>
					<argument name="pitch" type="float">
						The sound pitch, where a value of 1 is normal pitch.
					</argument>

					<seeAlso function="engine::sound::SoundContext.playSoundAt" />
				</playSound>
				<playSoundAt>
					<description>
						Plays a 3D sound, with attenuation and panning depending on where the listener is in the scene's 3D space.
					</description>
					<argument name="sound" type="AssetRef">
						The sound asset to play.
					</argument>
					<argument name="volume" type="float">
						The sound volume, where a value of 1 is how the volume was defined in the sound file.
					</argument>
					<argument name="pitch" type="float">
						The sound pitch, where a value of 1 is normal pitch.
					</argument>
					<argument name="position" type="engine::Vector3">
						The position in 3D space where the sound should be played.
					</argument>
					<argument name="range" type="float">
						The range of the sound in units.
					</argument>

					<seeAlso function="engine::sound::SoundContext.playSound" />
				</playSoundAt>
			</methods>
		</SoundContext>
	</types>
</docs>