using engine
using engine::input

module game

class MyObject : SceneObject
{
	override fn begin()
	{
		const testMesh = new MeshComponent()
		testMesh.load("cube")
		attach(testMesh)
	}

	override fn update()
	{
	}
}